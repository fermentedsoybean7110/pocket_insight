{
  "name": "pocket-insight",
  "version": "1.0.0",
  "description": "Full-stack expense tracker app - React + Express + MongoDB",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "seed": "npm run seed --prefix server",
    "lint": "concurrently \"npm run lint --prefix client\" \"npm run lint --prefix server\" || true",
    "format": "concurrently \"npm run format --prefix client\" \"npm run format --prefix server\" || true"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
